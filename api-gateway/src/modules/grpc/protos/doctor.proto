syntax = "proto3";

message Doctor {
  optional string id = 1;
  optional string payload = 2;
}

message Empty {}

service DoctorService {
  rpc AffiliateDoctor (Doctor) returns (Doctor);
  rpc GetDoctor (Doctor) returns (Doctor);
  rpc ListDoctors (Empty) returns (Doctor);
}
